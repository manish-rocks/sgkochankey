#include <cs50.h>
#include <stdio.h>
int main(void)
   {
      int x,n;
    printf("no");
    scanf("%i",&n);


    x=n%10;

    while(n!=0)
    {

    n=n/10;
    n!=0?(x=x*10+n%10):(x=x+n%10);
    }
    while(x!=0)
    {
       switch(x%10)
   {
   case 1:
   printf("one ");
   break;
   case 2:
   printf("two ");
   break;
   case 3:
   printf("three ");
   break;
   case 4:
   printf("four ");
   break;
   case 5:
   printf("five ");
   break;
   case 6:
   printf("six ");
   break;
   case 7:
   printf("seven ");
   break;
   case 8:
   printf("eight ");
   break;
   default:
   printf("nine ");
   break;

   }
   x=x/10;
    }
    if(n==0)
    printf("zero");
   }
